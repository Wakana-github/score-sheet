'use client';
import { DiceFiveIcon } from "@phosphor-icons/react";

export default function DiceOverlay() {
  return (
        <div>
          <DiceFiveIcon size={80} className="text-indigo-600 bounceIn" />
    </div>
  );
}